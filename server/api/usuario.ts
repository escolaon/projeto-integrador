export default defineEventHandler(() => {
    //consulte o banco
    //verifica se u usuario existe
    //se existir, retorna o usuario
    //se não existir, retorna um erro


    return {
        hello: 'API',
        
     }
})